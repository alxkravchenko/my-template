mixin icon(name,mod)
	if(mod)
		svg(class="svg-sprite-icon icon-" + name + ' ' + mod)
			use(xlink:href="./assets/images/sprite/symbol/sprite.svg#" + name)
	else
		svg
			use(xlink:href="./assets/images/sprite/symbol/sprite.svg#" + name)
